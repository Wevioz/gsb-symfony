<form method="post" action="{{ path('coeff_confiance_delete', {'coefconfNum': coeff_confiance.coefconfNum}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coeff_confiance.coefconfNum) }}">
    <button class="btn">Delete</button>
</form>
