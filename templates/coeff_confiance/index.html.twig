{% extends 'base.html.twig' %}

{% block title %}CoeffConfiance index{% endblock %}

{% block body %}
    <h1>CoeffConfiance index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>CoefconfNum</th>
                <th>CoefconfLibelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for coeff_confiance in coeff_confiances %}
            <tr>
                <td>{{ coeff_confiance.coefconfNum }}</td>
                <td>{{ coeff_confiance.coefconfLibelle }}</td>
                <td>
                    <a href="{{ path('coeff_confiance_show', {'coefconfNum': coeff_confiance.coefconfNum}) }}">show</a>
                    <a href="{{ path('coeff_confiance_edit', {'coefconfNum': coeff_confiance.coefconfNum}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('coeff_confiance_new') }}">Create new</a>
{% endblock %}
